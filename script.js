// **ส่วนที่ 2: โค้ด JavaScript** นำไปวางในไฟล์ใหม่ชื่อ "script.js"

// 1. กำหนดชุดคำถามและคำตอบที่ถูกต้อง
const quizData = [
    {
        question: "1. องค์กรใดที่รับผิดชอบการจัดสถานะความเสี่ยงของสัตว์ใกล้สูญพันธุ์ทั่วโลก?",
        options: ["WHO", "UNICEF", "IUCN", "NASA"],
        answer: "IUCN"
    },
    {
        question: "2. สาเหตุหลักอันดับต้นๆ ที่ทำให้สัตว์ใกล้สูญพันธุ์คืออะไร?",
        options: ["อุกกาบาตชน", "การทำลายถิ่นอาศัย (Habitat Loss)", "การเดินทางของมนุษย์", "การล่าปลาวาฬ"],
        answer: "การทำลายถิ่นอาศัย (Habitat Loss)"
    },
    {
        question: "3. สัตว์ส่วนใหญ่ที่อยู่ในสถานะ 'เสี่ยงต่อการสูญพันธุ์อย่างยิ่ง' (Critically Endangered) มีสาเหตุมาจากกิจกรรมใด?",
        options: ["ปรากฏการณ์เอลนีโญ", "การเปลี่ยนแปลงสภาพภูมิอากาศ", "มลพิษจากโรงงาน", "กิจกรรมของมนุษย์"],
        answer: "กิจกรรมของมนุษย์"
    },
    {
        question: "4. คำว่า 'Endangered Species' แปลเป็นภาษาไทยว่าอะไร?",
        options: ["สัตว์ครึ่งบกครึ่งน้ำ", "สัตว์เลี้ยงลูกด้วยนม", "สัตว์ใกล้สูญพันธุ์", "สัตว์ป่าสงวน"],
        answer: "สัตว์ใกล้สูญพันธุ์"
    },
    {
        question: "5. การล่าสัตว์เพื่อการค้าที่ผิดกฎหมาย (Poaching & Illegal Trade) ส่งผลกระทบต่อสัตว์ชนิดใดมากที่สุด?",
        options: ["แพนด้า", "สุนัขจิ้งจอก", "แรด/ช้าง/เสือ", "ปลาแซลมอน"],
        answer: "แรด/ช้าง/เสือ"
    },
    {
        question: "6. พลาสติกและขยะในทะเลส่วนใหญ่ส่งผลกระทบโดยตรงต่อสัตว์กลุ่มใด?",
        options: ["สัตว์บก", "สัตว์ทะเล", "สัตว์ปีก", "สัตว์เลี้ยง"],
        answer: "สัตว์ทะเล"
    },
    {
        question: "7. การเปลี่ยนแปลงสภาพภูมิอากาศ (Climate Change) ส่งผลให้สัตว์ชนิดใดเสี่ยงต่อการสูญพันธุ์เนื่องจากธารน้ำแข็งละลาย?",
        options: ["หมีขั้วโลก", "หมีแพนด้า", "หมีควาย", "หมีโคอาลา"],
        answer: "หมีขั้วโลก"
    },
    {
        question: "8. การตัดไม้ทำลายป่ามีผลกระทบต่อการรักษาสมดุลของระบบนิเวศอย่างไร?",
        options: ["ทำให้สัตว์มีที่อยู่เพิ่มขึ้น", "ลดความหลากหลายทางชีวภาพ", "เพิ่มปริมาณน้ำฝน", "ไม่ส่งผลกระทบใดๆ"],
        answer: "ลดความหลากหลายทางชีวภาพ"
    },
    {
        question: "9. วิธีใดที่เราในฐานะผู้บริโภคสามารถช่วยลดปัญหาสัตว์ใกล้สูญพันธุ์ได้?",
        options: ["ซื้อผลิตภัณฑ์ที่มาจากสัตว์ป่า", "สนับสนุนการใช้ถุงพลาสติก", "เลือกซื้อสินค้าที่เป็นมิตรต่อสิ่งแวดล้อม", "ละเลยข่าวสารด้านสิ่งแวดล้อม"],
        answer: "เลือกซื้อสินค้าที่เป็นมิตรต่อสิ่งแวดล้อม"
    },
    {
        question: "10. หากสัตว์ป่าที่ช่วยกระจายเมล็ดพันธุ์หายไป จะเกิดอะไรขึ้นกับป่าไม้?",
        options: ["ต้นไม้จะโตเร็วขึ้น", "ความสามารถในการแพร่กระจายของพืชจะลดลง", "อากาศจะเย็นลงทันที", "ไม่มีผลกระทบ"],
        answer: "ความสามารถในการแพร่กระจายของพืชจะลดลง"
    }
];

const quizContainer = document.getElementById('quiz-container');
const submitButton = document.getElementById('submit-quiz');
const resultDiv = document.getElementById('quiz-result');

// 2. ฟังก์ชันสำหรับสร้างคำถามบนหน้าเว็บ
function buildQuiz() {
    const output = [];
    
    quizData.forEach((currentQuestion, questionNumber) => {
        const options = [];
        
        // วนลูปสร้างปุ่มตัวเลือก
        for (const option of currentQuestion.options) {
            options.push(
                `<label style="display: block; margin: 10px 0;">
                    <input type="radio" name="question${questionNumber}" value="${option}">
                    ${option}
                </label>`
            );
        }

        // เพิ่มคำถามและตัวเลือกทั้งหมดลงใน output
        output.push(
            `<div class="question" style="margin-bottom: 20px; padding: 15px; border: 1px solid #ddd; border-radius: 5px;">
                <div class="question-text" style="font-weight: bold; margin-bottom: 10px;">${currentQuestion.question}</div>
                <div class="options">${options.join('')}</div>
            </div>`
        );
    });

    // แสดงคำถามทั้งหมดบนหน้าเว็บ
    quizContainer.innerHTML = output.join('');
}

// 3. ฟังก์ชันสำหรับตรวจคำตอบและแสดงผลลัพธ์
function checkAnswers() {
    const questionContainers = quizContainer.querySelectorAll('.question');
    let numCorrect = 0;

    quizData.forEach((currentQuestion, questionNumber) => {
        const questionContainer = questionContainers[questionNumber];
        const selector = `input[name=question${questionNumber}]:checked`;
        const userAnswerElement = (questionContainer.querySelector(selector) || {}).value;

        // ตรวจสอบคำตอบ
        if (userAnswerElement === currentQuestion.answer) {
            numCorrect++;
            // ทำเครื่องหมายเป็นสีเขียว
            questionContainer.style.backgroundColor = '#d4edda'; // สีเขียวอ่อน
        } else {
            // ทำเครื่องหมายเป็นสีแดง
            questionContainer.style.backgroundColor = '#f8d7da'; // สีแดงอ่อน
        }
    });

    // แสดงผลลัพธ์สุดท้าย
    resultDiv.style.padding = '15px';
    resultDiv.style.borderRadius = '5px';
    
    if (numCorrect === quizData.length) {
        // ได้คะแนนเต็ม
        resultDiv.innerHTML = `🎉 ยอดเยี่ยม! คุณตอบถูก **${numCorrect} ข้อ จาก ${quizData.length} ข้อ** คุณมีความรู้ด้านการอนุรักษ์ที่น่าทึ่งมาก!`;
        resultDiv.style.backgroundColor = '#eafaea';
        resultDiv.style.color = '#155724';
    } else if (numCorrect >= 2) {
        // ตอบถูกเกินครึ่ง
        resultDiv.innerHTML = `👍 ดีมาก! คุณตอบถูก **${numCorrect} ข้อ จาก ${quizData.length} ข้อ** พยายามอีกนิดเพื่อเป็นผู้เชี่ยวชาญ!`;
        resultDiv.style.backgroundColor = '#fff3cd';
        resultDiv.style.color = '#856404';
    } else {
        // ต้องปรับปรุง
        resultDiv.innerHTML = `💡 คุณตอบถูก **${numCorrect} ข้อ จาก ${quizData.length} ข้อ** มาเรียนรู้ข้อมูลเพิ่มเติมในเว็บไซต์นี้เพื่อช่วยโลกของเรานะคะ!`;
        resultDiv.style.backgroundColor = '#f8d7da';
        resultDiv.style.color = '#721c24';
    }
    
    // เลื่อนหน้าจอไปที่ผลลัพธ์
    resultDiv.scrollIntoView({ behavior: 'smooth' });
}
function resetQuiz() {
    const questionContainers = quizContainer.querySelectorAll('.question');
    const resultDiv = document.getElementById('quiz-result');
    const submitButton = document.getElementById('submit-quiz');

    // 1. ล้างสถานะสีแดง/เขียวจากการตรวจคำตอบเดิม
    questionContainers.forEach(container => {
        container.style.backgroundColor = 'white';
        // ล้างตัวเลือกที่ผู้ใช้เคยเลือกไว้
        const radioButtons = container.querySelectorAll('input[type="radio"]');
        radioButtons.forEach(radio => radio.checked = false);
    });

    // 2. ล้างผลลัพธ์ที่แสดง
    resultDiv.innerHTML = 'ผลลัพธ์จะแสดงที่นี่';
    resultDiv.style.backgroundColor = 'transparent';
    resultDiv.style.color = '#333';
    resultDiv.style.padding = '0';

    // 3. เปิดใช้งานปุ่มส่งคำตอบอีกครั้ง
    submitButton.disabled = false;
    submitButton.style.opacity = '1';

    // 4. เลื่อนหน้าจอไปที่จุดเริ่มต้นของ Quiz
    document.getElementById('quiz-section').scrollIntoView({ behavior: 'smooth' });
}
// เริ่มสร้าง Quiz ทันทีที่หน้าเว็บโหลด
buildQuiz();